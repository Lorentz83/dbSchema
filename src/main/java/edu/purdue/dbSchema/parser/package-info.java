/**
 * Contains the low level parser. Everything in this package deals only with SQL
 * syntax and is either code to parse or classes to contain the parsed result.
 *
 */
package edu.purdue.dbSchema.parser;
